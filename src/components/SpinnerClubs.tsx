import { ProgressSpinner, type ProgressSpinnerProps } from "primereact/progressspinner";

export default function SpinnerClubs(props: ProgressSpinnerProps) {
  return <ProgressSpinner {...props} />;
}
